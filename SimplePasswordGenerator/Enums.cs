namespace SimplePasswordGenerator
{
    /// <summary>
    /// Decides which casing the password will have.
    /// </summary>
    public enum Casing
    {
        Mixed = 0,
        Uppercase = 1,
        Lowercase = 2
    }
}
